import React from 'react';
import {View, StyleSheet} from 'react-native';

const Task = () => {
    return (
        <View>
            Task
        </View>
    );
}

const styles = StyleSheet.create({})

export default Task;
